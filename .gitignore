# Backend build artifacts
backend/target/
backend/.mvn/wrapper/maven-wrapper.jar

# Frontend files bundled into JAR during build (generated — do not commit)
backend/src/main/resources/static/

# Local environment secrets — NEVER commit
.env
.env.local
application-local.properties

# Uploads created at runtime (ephemeral on Render)
uploads/
backend/uploads/

# IDE
.idea/
*.iml
.vscode/

# OS
Thumbs.db
.DS_Store

# Quarantine — review before deleting permanently
# quarantine/    (uncomment to exclude from git; keep for now to review)
